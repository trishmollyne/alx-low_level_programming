#include "main.h"

/**
 * mul - you need t write a function that multiple intergers
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */

int mul(int a, int b)
{
int mul;
mul = a * b;
return (mul);
}
