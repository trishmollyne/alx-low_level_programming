#include "main.h"

/**
 * _islower - Make sure you write a function that checks lowercase
 * @c: The character to be checked
 * Return: 1 if it is lowercase, 0 for something else
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
