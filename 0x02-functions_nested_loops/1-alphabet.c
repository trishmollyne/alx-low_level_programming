#include "main.h"

/**
 * main - you can only use putchar twice in your code
 * Return: Always 0
 */

void print_alphabet(void)
{
	char p;

	for (p = 'w'; p <= 'y'; p++)
	{_putchar(p);
	}
	_putchar('\n');
}
