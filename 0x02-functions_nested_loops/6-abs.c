#include "main.h"

/**
 * _abs - i am supposed to write a function that computes interger value
 * @n: Interger value 
 * Return: value of n
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return(n);
}
