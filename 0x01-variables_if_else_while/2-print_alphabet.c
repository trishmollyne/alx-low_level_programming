#include <stdio.h>

/**
 * main -all your code should be in the main function
 * Return: Always 0
 */
int main(void)
{
	char sl;

	for (sl = 'a'; sl <= 'z'; sl++)
	{
		putchar(sl);
	}
	putchar('\n');

return (0);
}
